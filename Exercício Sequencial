/*1. Crie um algoritmo que calcule a soma e o produto de 3 números fornecidos pelo usuário. 
 #include <stdio.h>

int main(void) {
  int num1, num2, num3, res;
  printf("Digite 3 números para serem somados:\n");
  scanf("%d %d %d", &num1, &num2, &num3);
  res = num1 + num2 +num3;
  printf("Soma de %d + %d + %d é igual a %d", num1, num2, num3, res);
  return 0;
}
